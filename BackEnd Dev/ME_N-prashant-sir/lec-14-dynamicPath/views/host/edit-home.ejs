<%- include('../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>
    <main class="container mx-auto mt-8 p-8 bg-white rounded-lg shadow-md">
      <h1 class="text-3xl font-bold mb-6 text-center text-gray-800"><%= editing ? "Edit" : "Register" %> Your Home on AirBnB</h1>
      <form action="/host/<%= editing ? "edit-home" : "add-home" %>" method="POST" class="max-w-md mx-auto">
        <input type="hidden" name="id" value="<%= home ? home.id : '' %>" />
        <input
          type="text"
          name="houseName"
          placeholder="Enter your House Name"
          class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"
          value="<%= editing ? home.houseName : '' %>"
        />
        <input
          type="text"
          name="price"
          placeholder="Price Per Night"
          class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"
          value="<%= editing ? home.price : '' %>"
        />
        <input
          type="text"
          name="location"
          placeholder="Enter House Location"
          class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"
          value="<%= editing ? home.location : '' %>"
        />
        <input
          type="text"
          name="rating"
          placeholder="Enter House Rating"
          class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"
          value="<%= editing ? home.rating : '' %>"
        />
        <input
          type="text"
          name="photoUrl"
          placeholder="Enter House Img Url"
          class="w-full px-4 py-2 mb-4 border rounded-md focus:outline-none focus:ring-2 focus:ring-red-500"
          value="<%= editing ? home.photoUrl : '' %>"
        />
        <input type="submit" value="<%= editing ? "Update Home" : "Submit" %>" class="w-full bg-red-500 text-white py-2 rounded-md hover:bg-red-600 transition duration-300" />
      </form>
    </main>
  </body>
</html>